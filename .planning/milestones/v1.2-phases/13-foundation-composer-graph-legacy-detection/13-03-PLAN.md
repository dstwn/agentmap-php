---
phase: 13-foundation-composer-graph-legacy-detection
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - src/Core/map-builder.mjs
autonomous: true
requirements:
  - CMP-01
  - CMP-02
  - CMP-03
  - LEG-01
  - LEG-02

must_haves:
  truths:
    - "map.json contains top-level 'packages' key with composer dependency edges after build()"
    - "map.json contains top-level 'legacyWarnings' key with heuristic warnings after build()"
    - "build() completes successfully when composer files are absent (empty packages and legacyWarnings arrays)"
    - "build() completes successfully when composer files are present and populates both keys"
    - "All pre-existing build() behavior (files, edges, hubs, features, rankedSymbols, pagerank) is unchanged"
    - "Full test suite (node --test test/) passes with no regressions"
  artifacts:
    - path: "src/Core/map-builder.mjs"
      provides: "Integrated ComposerParser + LegacyDetector calls in build()"
      contains: "ComposerParser"
  key_links:
    - from: "src/Core/map-builder.mjs"
      to: "src/Core/ComposerParser.mjs"
      via: "named import; called as new ComposerParser().parse(cwd) before writeFileSync"
      pattern: "ComposerParser"
    - from: "src/Core/map-builder.mjs"
      to: "src/Core/LegacyDetector.mjs"
      via: "named import; called as new LegacyDetector().detect(cwd, ...) before writeFileSync"
      pattern: "LegacyDetector"
    - from: "src/Core/map-builder.mjs"
      to: "map.json (out object)"
      via: "packages and legacyWarnings appended to out before JSON.stringify"
      pattern: "packages.*composerResult|legacyWarnings"
---

<objective>
Wire ComposerParser and LegacyDetector into map-builder.mjs build() and verify the full phase with the complete test suite.

Purpose: This is the integration layer — two import lines and two call sites in build() — plus a final regression gate confirming all 194+ existing tests still pass alongside the new composer-parser tests.

Output: src/Core/map-builder.mjs (modified — imports + two call sites + two new out keys)
</objective>

<execution_context>
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/workflows/execute-plan.md
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Core/map-builder.mjs
@src/Core/ComposerParser.mjs
@src/Core/LegacyDetector.mjs
@.planning/phases/13-foundation-composer-graph-legacy-detection/13-01-SUMMARY.md
@.planning/phases/13-foundation-composer-graph-legacy-detection/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ComposerParser and LegacyDetector into build()</name>
  <files>src/Core/map-builder.mjs</files>
  <read_first>
    - src/Core/map-builder.mjs — read entire file (355 lines); identify the import block (lines 1–9) and the out object construction (lines 344–348) and writeFileSync call (lines 349–352)
    - src/Core/ComposerParser.mjs — confirm parse(projectRoot) return shape: {packages, psr4Map, classmaps, autoFiles}
    - src/Core/LegacyDetector.mjs — confirm detect(projectRoot, psr4Map, classmaps, autoFiles) return shape: legacyWarnings[]
  </read_first>
  <action>
    Make exactly three edits to src/Core/map-builder.mjs — no other lines touched:

    Edit 1 — Add two imports at the end of the existing import block (after line 9, before the blank line separating imports from featureOf):
      import { ComposerParser } from "./ComposerParser.mjs";
      import { LegacyDetector } from "./LegacyDetector.mjs";

    Edit 2 — Add two call sites inside build(), immediately before the construction of the out object (before the `const out = {` line at line 344). Insert:
      const composerResult = new ComposerParser().parse(cwd);
      const legacyWarnings = new LegacyDetector().detect(
        cwd, composerResult.psr4Map, composerResult.classmaps, composerResult.autoFiles
      );

    Edit 3 — Extend the out object to include the two new keys. The existing out object ends with `files,`. Add two new entries after files:
      packages: composerResult.packages,
      legacyWarnings,

    The resulting out object shape: {schema, generatedSha, dirty, fileCount, fingerprint, hubs, features, rankedSymbols, files, packages, legacyWarnings}.

    No other changes. Do not modify makeProject(), resolveSpec(), featureOf(), or any existing logic. The schema value stays SCHEMA_VERSION (3) — version bump is Phase 16 (per D-01 in CONTEXT.md).

    cwd is already defined in build() as `process.cwd().replace(/\\/g, "/")` — use it directly, no redefinition.
  </action>
  <verify>
    <automated>node --test test/composer-parser.test.mjs && node --test test/</automated>
  </verify>
  <acceptance_criteria>
    - src/Core/map-builder.mjs imports ComposerParser from "./ComposerParser.mjs" and LegacyDetector from "./LegacyDetector.mjs"
    - build() calls new ComposerParser().parse(cwd) and assigns result to composerResult
    - build() calls new LegacyDetector().detect(cwd, composerResult.psr4Map, composerResult.classmaps, composerResult.autoFiles) and assigns to legacyWarnings
    - out object has keys packages (array) and legacyWarnings (array)
    - SCHEMA_VERSION value in out.schema is still 3 (not bumped — Phase 16 responsibility)
    - All existing exports of map-builder.mjs (build, featureOf, makeProject, readTsconfigAliasOpts, discoverPackageAliasConfigs, nearestAliasConfig) remain intact
    - node --test test/ exits 0 (no regressions in any of the 194+ existing tests)
    - node --test test/composer-parser.test.mjs exits 0 (all new tests pass)
  </acceptance_criteria>
  <done>map-builder.mjs build() populates packages and legacyWarnings in map.json output; full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build() → ComposerParser/LegacyDetector | Caller is the local CLI build step; projectRoot is process.cwd() — trusted local path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | map-builder.mjs out object | accept | packages and legacyWarnings are appended as new keys; existing keys (files, hubs, etc.) are not modified; atomic rename via tmp file already in place |
| T-13-07 | DoS | ComposerParser called in every build() | accept | ComposerParser is two readFileSync + JSON.parse calls — negligible overhead; no loop over project files |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No external packages; this plan only edits map-builder.mjs |
</threat_model>

<verification>
After Task 1 completes:

1. `node --test test/composer-parser.test.mjs` — exits 0
2. `node --test test/` — exits 0 (all existing + new tests pass)
3. Spot-check map.json shape:
   ```
   node -e "
     import('./src/Core/map-builder.mjs').then(m => {
       const out = m.build();
       console.assert(Array.isArray(out.packages), 'packages missing');
       console.assert(Array.isArray(out.legacyWarnings), 'legacyWarnings missing');
       console.assert(out.schema === 3, 'schema must stay 3');
       console.log('integration ok — packages:', out.packages.length, 'legacyWarnings:', out.legacyWarnings.length);
     });
   "
   ```
   Output should include "integration ok" with numeric counts (0 is valid if no composer.json in project root).
</verification>

<success_criteria>
- map-builder.mjs build() imports and calls ComposerParser and LegacyDetector
- map.json out object has packages and legacyWarnings top-level keys
- schema stays at 3 (version bump deferred to Phase 16 per D-01)
- node --test test/ — all tests green, no regressions
- node --test test/composer-parser.test.mjs — all composer/legacy/PSR4 tests green
</success_criteria>

<output>
Create `.planning/phases/13-foundation-composer-graph-legacy-detection/13-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Type | File | Notes |
|--------|------|------|-------|
| build() (modified) | function | src/Core/map-builder.mjs | Now calls ComposerParser + LegacyDetector; out has packages + legacyWarnings keys |
| map.json packages key | JSON field | .claude/agentmap/map.json | Array of {from, to, type, constraint, resolvedVersion} entries |
| map.json legacyWarnings key | JSON field | .claude/agentmap/map.json | Array of {type, directory|entry, message} entries |
