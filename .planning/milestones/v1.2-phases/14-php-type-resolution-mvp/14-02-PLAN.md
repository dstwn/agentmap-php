---
phase: 14-php-type-resolution-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/Core/map-builder.mjs
autonomous: true
requirements:
  - TYP-01
  - TYP-02
must_haves:
  truths:
    - "After map rebuild, each PHP file entry in map.json contains assignedTypes array (D-01)"
    - "After map rebuild, each PHP file entry in map.json contains phpDocTypes array (D-01)"
    - "declaredTypes from inferTypes() still present — TypeResolver does not replace it (D-01)"
    - "Non-PHP file entries are unaffected — no assignedTypes/phpDocTypes keys added to JS/TS files"
    - "Map rebuild completes without error when composer.json is absent (psr4Map empty)"
    - "Map rebuild on laravel/framework bench adds less than 200ms vs baseline"
  artifacts:
    - path: "src/Core/map-builder.mjs"
      provides: "TypeResolver integration — assignedTypes + phpDocTypes merged onto each PHP file entry"
      contains: "TypeResolver"
  key_links:
    - from: "src/Core/map-builder.mjs"
      to: "src/Core/TypeResolver.mjs"
      via: "import TypeResolver; call typeResolver.resolve(path, text, psr4Map, cwd) per PHP file"
      pattern: "typeResolver\\.resolve"
    - from: "src/Core/map-builder.mjs"
      to: "src/Core/EnhancedLaravelParser.mjs"
      via: "inferTypes() output remains on entry.types — TypeResolver adds separate arrays"
      pattern: "entry\\.assignedTypes"
---

<objective>
Wire TypeResolver into map-builder.mjs so every PHP file entry in map.json gains assignedTypes and phpDocTypes arrays alongside existing declared types (TYP-01, TYP-02).

Purpose: Closes the integration gap — TypeResolver exists but has no effect until map-builder calls it and persists results to map.json.

Output: Modified src/Core/map-builder.mjs; map.json gains assignedTypes + phpDocTypes on PHP entries.
</objective>

<execution_context>
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/workflows/execute-plan.md
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-php-type-resolution-mvp/14-CONTEXT.md
@.planning/phases/14-php-type-resolution-mvp/14-RESEARCH.md
@.planning/phases/14-php-type-resolution-mvp/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TypeResolver import and PHP file loop to map-builder.mjs</name>
  <files>src/Core/map-builder.mjs</files>
  <read_first>
    - src/Core/map-builder.mjs — full file (364 lines): import block (lines 1-11), build() function, specifically lines 344-364 (ComposerParser/LegacyDetector calls + out object construction)
    - src/Core/TypeResolver.mjs — just built in 14-01: resolve() signature and return shape
    - .planning/phases/14-php-type-resolution-mvp/14-RESEARCH.md — Pattern 5 (map-builder integration, lines 328-352) and anti-patterns (vendor exclusion, relative path storage)
  </read_first>
  <behavior>
    Integration contract:
    - import { TypeResolver } from "./TypeResolver.mjs" added to the import block at top of file (alongside ComposerParser, LegacyDetector)
    - A single TypeResolver instance created once inside build(), after composerResult is obtained (so psr4Map is available)
    - Loop over Object.entries(files) — for each entry where path ends with ".php": read file text via readFileSync(path, "utf8"), call typeResolver.resolve(path, text, composerResult.psr4Map, cwd), assign entry.assignedTypes = assignedTypes and entry.phpDocTypes = phpDocTypes; wrap in try-catch assigning empty arrays on error
    - Non-.php entries: no assignedTypes or phpDocTypes keys added
    - The existing `out` object construction and all other logic unchanged
    - vendor/ files: skip any path containing "/vendor/" in the loop (matches existing pattern for vendor exclusion)
  </behavior>
  <action>
    Edit src/Core/map-builder.mjs in two scoped changes:

    Change 1 — Add import at line 11 (after the LegacyDetector import):
    Add: import { TypeResolver } from "./TypeResolver.mjs";

    Change 2 — Add TypeResolver loop after line 348 (after legacyWarnings is assigned, before the `const sha` line):
    Insert the following block between the legacyWarnings assignment and `const sha = currentSha()`:

    Instantiate a single TypeResolver: const typeResolver = new TypeResolver();
    Then iterate Object.entries(files): for each [filePath, entry] pair, skip if filePath does not end with ".php" OR if filePath includes "/vendor/". Inside the loop: wrap in try-catch. In try: const text = readFileSync(filePath, "utf8"); const { assignedTypes, phpDocTypes } = typeResolver.resolve(filePath, text, composerResult.psr4Map || {}, cwd); entry.assignedTypes = assignedTypes; entry.phpDocTypes = phpDocTypes; In catch: entry.assignedTypes = []; entry.phpDocTypes = [];

    Do NOT modify the `out` object — assignedTypes and phpDocTypes are stored on individual file entries inside `files`, which is already part of `out.files`. No new top-level key needed.

    Use readFileSync which is already imported at line 1 of map-builder.mjs — no new imports needed beyond TypeResolver.

    After editing, run: node --test test/type-resolver.test.mjs to confirm TypeResolver unit tests still pass.
    Commit: feat(14-02): wire TypeResolver into map-builder — assignedTypes + phpDocTypes on PHP entries
  </action>
  <verify>
    <automated>node --test test/type-resolver.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <done>TypeResolver imported in map-builder.mjs. node --test test/type-resolver.test.mjs passes. No syntax errors in map-builder.mjs (node --check src/Core/map-builder.mjs exits 0).</done>
</task>

<task type="auto">
  <name>Task 2: Integration smoke test — map.json gains assignedTypes + phpDocTypes on PHP entries</name>
  <files></files>
  <read_first>
    - No source edits — pure verification
  </read_first>
  <action>
    Run the full agentmap build against the project itself and verify map.json output:

    Step 1 — Build the map:
    node src/index.mjs --map 2>&1 | tail -3
    (or whatever the project's map-build CLI invocation is — check package.json scripts if unsure)

    Step 2 — Check a PHP file entry has the new keys:
    node -e "const m=JSON.parse(require('fs').readFileSync('.claude/agentmap/map.json','utf8')); const phpEntry=Object.entries(m.files).find(([k])=>k.endsWith('.php')); if(!phpEntry){console.log('NO PHP ENTRIES');process.exit(1);} const [p,e]=phpEntry; console.log(p,'assignedTypes:',Array.isArray(e.assignedTypes),'phpDocTypes:',Array.isArray(e.phpDocTypes));"

    Step 3 — Confirm declared types still present on the same entry:
    node -e "const m=JSON.parse(require('fs').readFileSync('.claude/agentmap/map.json','utf8')); const phpEntry=Object.entries(m.files).find(([k,v])=>k.endsWith('.php')&&v.types&&v.types.length>0); if(!phpEntry){console.log('No PHP entry with types found — may be OK if no typed PHP in project');process.exit(0);} const [p,e]=phpEntry; console.log(p,'types.length:',e.types.length,'assignedTypes.length:',e.assignedTypes.length,'phpDocTypes.length:',e.phpDocTypes.length);"

    Step 4 — Run full regression suite:
    node --test test/composer-parser.test.mjs test/enhanced-laravel.test.mjs test/php-parser.test.mjs test/type-resolver.test.mjs
  </action>
  <verify>
    <automated>node --test test/composer-parser.test.mjs test/enhanced-laravel.test.mjs test/php-parser.test.mjs test/type-resolver.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Full test suite (4 files) exits 0, no regressions
    - map.json PHP entries have assignedTypes (array) and phpDocTypes (array) keys
    - Existing types array on PHP entries is unaffected
    - Non-PHP entries have no assignedTypes or phpDocTypes keys
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PHP source file content → readFileSync in map-builder loop | File read in build() context; untrusted content passed to TypeResolver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Denial of Service | map-builder PHP loop — readFileSync on large files | accept | Same risk exists for all other file reads in build(); TypeResolver try-catch returns empty arrays on any error |
| T-14-05 | Tampering | vendor/ files leaking into assignedTypes | mitigate | Loop skips paths containing "/vendor/" — consistent with existing vendor exclusion in map-builder |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages — TypeResolver uses only tree-sitter already installed |
</threat_model>

<verification>
- node --check src/Core/map-builder.mjs exits 0 (syntax valid)
- node --test test/composer-parser.test.mjs test/enhanced-laravel.test.mjs test/php-parser.test.mjs test/type-resolver.test.mjs — all pass
- map.json PHP file entries contain assignedTypes and phpDocTypes arrays
- map.json non-PHP entries do NOT contain assignedTypes or phpDocTypes keys
- Declared types (entry.types) still present on PHP entries — TypeResolver did not replace inferTypes() output
</verification>

<success_criteria>
- map-builder.mjs imports and calls TypeResolver once per PHP file after composerResult is available
- Each PHP file entry in map.json has assignedTypes[] and phpDocTypes[] alongside existing types[]
- vendor/ PHP files excluded from TypeResolver pass
- Full 4-file test suite green, no pre-existing test regressions
- node --map completes without error on a project with no composer.json (psr4Map defaults to {})
</success_criteria>

<output>
Create .planning/phases/14-php-type-resolution-mvp/14-02-SUMMARY.md when done

## Artifacts this phase produces
- src/Core/map-builder.mjs — modified: TypeResolver import added; PHP file loop added after legacyWarnings; entry.assignedTypes and entry.phpDocTypes populated per PHP file
</output>
