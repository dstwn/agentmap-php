---
phase: 15-advanced-type-resolution
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/Core/constants.mjs
  - test/type-resolver.test.mjs
  - test/fixtures/chains.php
autonomous: true
requirements:
  - TYP-03
  - TYP-04

must_haves:
  truths:
    - "constants.mjs exports DEFAULT_CHAIN_DEPTH = 3"
    - "test/fixtures/chains.php contains 1-level, 2-level, 3-level, and >3-level chain fixtures with class definitions"
    - "type-resolver.test.mjs has RED failing tests for resolveChain() and confidence backfill of enhanced.types"
    - "All existing 15 tests still pass (no regression)"
  artifacts:
    - path: "src/Core/constants.mjs"
      provides: "DEFAULT_CHAIN_DEPTH constant"
      contains: "DEFAULT_CHAIN_DEPTH"
    - path: "test/fixtures/chains.php"
      provides: "PHP fixtures for 1/2/3/>3-level chains"
      min_lines: 60
    - path: "test/type-resolver.test.mjs"
      provides: "Failing tests for TYP-03 (chain tracing) and TYP-04 (confidence backfill)"
  key_links:
    - from: "test/type-resolver.test.mjs"
      to: "src/Core/TypeResolver.mjs"
      via: "resolveChain() call — must fail RED (method does not exist yet)"
      pattern: "resolveChain"
    - from: "test/fixtures/chains.php"
      to: "test/type-resolver.test.mjs"
      via: "readFileSync fixture load — same pattern as assignments.php/phpdoc.php"
      pattern: "chains\\.php"
---

<objective>
Lay the TDD RED foundation for Phase 15: add DEFAULT_CHAIN_DEPTH to constants.mjs, create chains.php fixture covering all four depth cases, and write failing tests for resolveChain() chain tracing (TYP-03) and enhanced.types confidence backfill (TYP-04).

Purpose: Tests must fail RED before implementation begins — this is the TDD contract. Plan 2 makes them GREEN.
Output: constants.mjs with DEFAULT_CHAIN_DEPTH, chains.php fixture, extended test/type-resolver.test.mjs with 9+ new RED tests.
</objective>

<execution_context>
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/workflows/execute-plan.md
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-php-type-resolution-mvp/14-01-SUMMARY.md
@.planning/phases/14-php-type-resolution-mvp/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DEFAULT_CHAIN_DEPTH constant + create chains.php fixture</name>
  <files>src/Core/constants.mjs, test/fixtures/chains.php</files>

  <read_first>
    - src/Core/constants.mjs — read all 27 lines; append after LEGACY_DIRS line
    - test/fixtures/assignments.php — read to understand fixture PHP style (opening tag, class definitions, body)
    - test/fixtures/phpdoc.php — read to understand fixture PHP style
  </read_first>

  <action>
**constants.mjs:** Append one export after the LEGACY_DIRS line:
  `export const DEFAULT_CHAIN_DEPTH = 3;`
  No other changes to the file.

**test/fixtures/chains.php:** Create a single PHP file with four sections. Each section defines the classes needed for that chain depth. Use `<?php` opening tag. No namespace declarations needed — chains.php uses bare class names so tests can call resolveChain() with a pre-built useMap.

Section 1 — 1-level chain fixture:
  Class `Builder1` with method `build()` returning `Result1`.
  Class `Result1` with property `$value` (no chain methods).
  Variable assignment: `$r1 = $b1->build();`

Section 2 — 2-level chain fixture:
  Class `Builder2` with method `step1()` returning `Step2`.
  Class `Step2` with method `step2()` returning `Result2`.
  Class `Result2` with property `$done`.
  Variable assignment: `$r2 = $b2->step1()->step2();`

Section 3 — 3-level chain fixture (at exact depth limit):
  Class `Builder3` with method `a()` returning `Mid3A`.
  Class `Mid3A` with method `b()` returning `Mid3B`.
  Class `Mid3B` with method `c()` returning `Result3`.
  Class `Result3` with property `$end`.
  Variable assignment: `$r3 = $b3->a()->b()->c();`

Section 4 — >3-level chain fixture (exceeds DEFAULT_CHAIN_DEPTH):
  Class `Builder4` with method `w()` returning `Step4A`.
  Class `Step4A` with method `x()` returning `Step4B`.
  Class `Step4B` with method `y()` returning `Step4C`.
  Class `Step4C` with method `z()` returning `Result4`.
  Class `Result4` with property `$final`.
  Variable assignment: `$r4 = $b4->w()->x()->y()->z();`

Each class definition must have a valid declared return type on the chain method (e.g., `public function build(): Result1`). This is critical so _findMethodReturnType() can extract the return type from the class AST. No use-import declarations needed — bare class names in this fixture.

Inline the class definitions AND the variable assignments in the same file so that when chains.php text is parsed by TypeResolver, both _extractAssignments() and resolveChain() can operate on it. Wrap assignments in a function body to be valid PHP (e.g., `function testChains() { ... }`).
  </action>

  <verify>
    <automated>node -e "import('./src/Core/constants.mjs').then(m => { if (m.DEFAULT_CHAIN_DEPTH !== 3) throw new Error('DEFAULT_CHAIN_DEPTH not 3'); console.log('OK'); })"</automated>
  </verify>

  <acceptance_criteria>
    - `DEFAULT_CHAIN_DEPTH` exported from constants.mjs with value 3
    - test/fixtures/chains.php exists with all four depth sections (1-level, 2-level, 3-level, >3-level)
    - Each chain class has a declared return type on its chain method
    - File is syntactically valid PHP (no parse errors from tree-sitter)
  </acceptance_criteria>

  <done>constants.mjs has DEFAULT_CHAIN_DEPTH = 3; chains.php has four chain depth fixtures with declared return types</done>
</task>

<task type="tdd">
  <name>Task 2: Write RED failing tests for resolveChain() and confidence backfill (TYP-03 + TYP-04)</name>
  <files>test/type-resolver.test.mjs</files>

  <read_first>
    - test/type-resolver.test.mjs — read all 157 lines; append new describe blocks after the graceful degradation block
    - test/fixtures/chains.php — read the file just created in Task 1
    - src/Core/TypeResolver.mjs — read all 168 lines; confirm resolveChain does NOT exist (tests will fail RED)
    - .planning/phases/15-advanced-type-resolution/15-RESEARCH.md lines 42-48 (phase requirements table) and lines 503-529 (test map + Wave 0 gaps)
  </read_first>

  <action>
Append two new describe blocks to test/type-resolver.test.mjs after the existing "graceful degradation" describe block. Do NOT modify any existing tests.

Add at the top of the file (after existing fixture reads): load chains.php text with readFileSync using the same pattern as assignmentsPhp. Store as `chainsPhp` and `chainsFixturePath`.

**Describe block 1: "TypeResolver — TYP-03 resolveChain() method chain tracing"**

Nine tests. For all resolveChain() tests, call `typeResolver.resolveChain(root, useMap, assignedTypes, psr4Map, projectRoot, depthLimit)`. Since resolveChain() does not exist yet, every test will throw TypeError and fail RED.

To set up the call without needing the real method, wrap each test as:
  ```
  // Will fail RED: resolveChain does not exist yet
  assert.ok(typeof typeResolver.resolveChain === "function", "resolveChain must be a method");
  ```
This single assertion per test is sufficient to produce clean RED failures. No need to build full AST fixtures in Plan 1 — the method existence check catches the RED state.

Test names (9 total):
  1. "resolveChain() method exists on TypeResolver instance" — assert typeof resolveChain === "function"
  2. "1-level chain: $r1 = $b1->build() → chainTypes entry with resolvedType Result1, confidence LOW" — assert resolveChain is a function (RED)
  3. "2-level chain: $r2 = $b2->step1()->step2() → resolvedType Result2, confidence LOW" — assert resolveChain is a function (RED)
  4. "3-level chain at depth limit → resolvedType Result3, confidence LOW, no warning skipped" — assert resolveChain is a function (RED)
  5. ">3-level chain truncated at depth 3 → confidence LOW, stderr warning emitted" — assert resolveChain is a function (RED)
  6. "cycle detection: revisiting same class::method stops chain, returns LOW confidence" — assert resolveChain is a function (RED)
  7. "unknown return type mid-chain → chain stops, partial result with confidence LOW" — assert resolveChain is a function (RED)
  8. "empty psr4Map → resolveChain returns empty array (graceful degradation)" — assert resolveChain is a function (RED)
  9. "chainTypes entries carry source: chain" — assert resolveChain is a function (RED)

**Describe block 2: "TypeResolver — TYP-04 confidence backfill (enhanced.types)"**

Two tests for the agentmap.mjs backfill behavior. These tests verify the SHAPE expected after backfill — they assert against the TypeResolver resolve() output to confirm chain-related confidence fields. Per D-01 (TYP-04), the backfill of enhanced.types happens in agentmap.mjs, not in TypeResolver itself. So these tests verify:
  1. "resolve() assignedTypes entries carry confidence MEDIUM and no source field required" — this passes GREEN already (Phase 14); include as regression anchor
  2. "resolveChain() chainTypes entries carry confidence LOW and source chain" — assert resolveChain is a function (RED)

**Run tests to confirm RED state:**
`node --test test/type-resolver.test.mjs`
Confirm new tests fail with "resolveChain must be a method" / AssertionError. Confirm all 15 pre-existing tests still pass.

Commit message: `test(15-01): add RED failing tests for resolveChain() chain tracing and confidence (TYP-03/TYP-04)`
  </action>

  <verify>
    <automated>node --test test/type-resolver.test.mjs 2>&1 | grep -E "^(pass|fail)" | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - All 15 pre-existing tests pass (no regression)
    - All 10 new chain/confidence tests fail RED (resolveChain not a function)
    - Test output shows exactly the new describe blocks listed above
    - Commit tagged test(15-01) exists
  </acceptance_criteria>

  <done>25 total tests: 15 GREEN (existing) + 10 RED (new). Commit test(15-01) exists. RED state confirmed before Plan 2 implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → parser | chains.php content parsed by tree-sitter; malformed PHP could panic parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | chains.php fixture | accept | Fixture is developer-authored, version-controlled; no user input path |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `node -e "import('./src/Core/constants.mjs').then(m => console.log(m.DEFAULT_CHAIN_DEPTH))"` prints `3`
- `node --test test/type-resolver.test.mjs` shows 15 pass + 10 fail (RED)
- No new packages installed
</verification>

<success_criteria>
- DEFAULT_CHAIN_DEPTH = 3 exported from constants.mjs
- chains.php fixture with all four depth sections exists
- 10 new RED tests committed; 15 existing tests still green
- Plan 2 can proceed directly to implementation (GREEN phase)
</success_criteria>

<output>
Create `.planning/phases/15-advanced-type-resolution/15-01-SUMMARY.md` when done
</output>
