---
phase: 16-integration-cli
plan: 03
type: execute
wave: 2
depends_on:
  - 16-02
files_modified:
  - agentmap.mjs
autonomous: true
requirements:
  - CMP-04
  - CMP-05

must_haves:
  truths:
    - "Package nodes appear in --print output with a numeric pagerank field after build"
    - "Package→file edges are capped at 1000 per package; stderr warns when truncated"
    - "Edge weight is 0.1× the average direct import weight (subtle boost, not dominant)"
    - "--any with a package name surfaces package matches before symbol matches"
    - "Vendor files are never added to files{} or nodes[] (vendor/ exclusion preserved)"
    - "All pre-existing tests remain green"
  artifacts:
    - path: "agentmap.mjs"
      provides: "Package→file synthetic node edges in build(); --any package injection"
      contains: "PKG_EDGE_CAP"
  key_links:
    - from: "agentmap.mjs build() TypeResolver block (~line 784)"
      to: "agentmap.mjs fileEdges array (~line 801)"
      via: "package→synthetic-node edges appended to fileEdges before pagerank() call"
      pattern: "PKG_EDGE_CAP"
    - from: "agentmap.mjs --any handler pkgMatches"
      to: "data.packages array"
      via: "filter on p.to.toLowerCase() or p.from.toLowerCase() includes query"
      pattern: "pkgMatches"
    - from: "agentmap.mjs build() out object (~line 823)"
      to: "packages array with pagerank field"
      via: "packages entries annotated with pagerank score from fileRank[pkgNode]"
      pattern: "pkgNode"
---

<objective>
Merge package→file PageRank edges into agentmap.mjs build() (CMP-04) and inject package
name lookup into the --any router (CMP-05 partial). Package names become synthetic nodes
in the PageRank graph, receiving a subtle rank boost from dependent PHP source files.

Purpose: Makes packages first-class citizens in the rank graph; powers --any package routing.
Output: Updated agentmap.mjs — build() package edge block + --any pkgMatches injection
</objective>

<execution_context>
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/workflows/execute-plan.md
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agentmap.mjs
@.planning/phases/16-integration-cli/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add package→synthetic-node PageRank edges in build() (CMP-04)</name>
  <files>agentmap.mjs</files>
  <read_first>
    - agentmap.mjs lines 748-806 — full TypeResolver+pagerank block: exact scope of composerResult,
      psr4Map, fileEdges array, pagerank() call, nodes array
    - agentmap.mjs lines 818-836 — out object construction: where packages array is assembled
      (confirm packages comes from map-builder result, not built here)
    - agentmap.mjs lines 935-970 — ensureFresh(): confirm it returns the build() out object
      so data.packages is available to handlers
    - .planning/phases/16-integration-cli/16-RESEARCH.md lines 131-166 — CMP-04 exact
      integration point, synthetic node approach, vendor exclusion constraint, Pitfall 1+4
  </read_first>
  <action>
    LOCATION: Insert new block in agentmap.mjs build() AFTER the TypeResolver enrichment
    block ends (after the closing brace of the for-loop at ~line 784) and BEFORE the
    "// 7: resolve default-import edges" comment (line 786). This places package edges in
    fileEdges before pagerank() at line 805.

    STEP 1 — Define PKG_EDGE_CAP constant near the top of agentmap.mjs (after existing
    constants like DEFAULT_SYMBOLS, around line 67-70):
      const PKG_EDGE_CAP = 1000;

    STEP 2 — Inside build(), after the TypeResolver for-loop closes, insert a new block:

    Get the composer result: the psr4Map variable is already in scope (from the try-catch
    at lines 750-754). Re-use the same ComposerParser result already parsed. Extract
    packages array: call cp.parse(process.cwd()) result is already in parsed local var —
    but note parsed is declared inside the try block. To access packages in the edge block,
    move the packages array extraction outside the try scope OR re-call cp.parse() with
    try-catch. Simplest: declare a "let composerPackages = [];" before the try block, then
    inside the try: "composerPackages = parsed?.packages ?? [];".

    STEP 3 — After the TypeResolver for-loop, build package edges:

    For each package in composerPackages where package.type === "require" (not require-dev,
    not conflict etc.):
    - The package name is package.to (e.g. "laravel/framework")
    - Create a synthetic node key: "pkg:" + package.to (e.g. "pkg:laravel/framework")
    - Find all PHP source files in files{} (keys ending in ".php", not containing "/vendor/")
      that are considered dependent on this package. Since we have no per-file package usage
      data at this point, use ALL non-vendor PHP source files as dependents (they all
      transitively benefit from the package). Cap the list at PKG_EDGE_CAP entries
      (sort alphabetically first, take first PKG_EDGE_CAP).
    - If the full list exceeds PKG_EDGE_CAP, write to stderr:
        process.stderr.write("# agentmap: package edge cap hit for " + package.to + ": " +
        fullList.length + " files truncated to " + PKG_EDGE_CAP + "\n")
    - Compute edge weight: 0.1 × (average weight of existing fileEdges, or 1.0 if no edges).
      Average weight = fileEdges.length > 0
        ? fileEdges.reduce((s, e) => s + e.weight, 0) / fileEdges.length
        : 1.0;
      pkgWeight = 0.1 * avgWeight; clamp to minimum 0.01.
    - Add synthetic node to nodes array: nodes.push(pkgNode) if not already present.
    - Add edges: for each PHP file in the capped list, push
        { from: phpFile, to: pkgNode, weight: pkgWeight }
      to fileEdges.

    STEP 4 — After pagerank() call (after line 806 "for (const p of nodes) files[p].pagerank..."),
    annotate the packages array with their pagerank scores. The out object at line 823 includes
    "hubs, features, rankedSymbols, files" — packages come from the map-builder result.
    Find where packages is assembled in the out object. Add pagerank annotation:

    Before the out object literal, add:
      const pkgRankMap = {};
      for (const pkg of (composerPackages || [])) {
        const pkgNode = "pkg:" + pkg.to;
        pkgRankMap[pkg.to] = +(fileRank[pkgNode] || 0).toFixed(6);
      }

    Then in the out object literal, replace the packages line so each package entry gets
    a pagerank field. The packages array comes from the map-builder result (accessed via
    the existing build flow — read the actual out object lines to find it). Add it as:
      packages: (composerPackages || []).map(p => ({ ...p, pagerank: pkgRankMap[p.to] ?? 0 }))

    CRITICAL constraints:
    - Do NOT add vendor/ files to files{} or nodes[]. Only add "pkg:{name}" synthetic keys.
    - Synthetic "pkg:" nodes must NOT appear in hubs output or rankedSymbols — they are
      package-only. After computing pagerank and annotating packages, delete pkg: nodes
      from nodes array before hubs/rankedSymbols computation. Do this by filtering:
        const fileNodes = nodes.filter(n => !n.startsWith("pkg:"));
      Then use fileNodes for hubs, rankedSymbols, and the "delete files[p].defaultExportName"
      cleanup loop. Keep nodes (with pkg: entries) only for the pagerank() call.
    - Edge insertion happens BEFORE pagerank() call at line 805 — verify ordering.
    - composerPackages edge block must be wrapped in try-catch: on any error, skip silently
      (package edges are a boost, not critical path).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    npm test passes. node agentmap.mjs --print outputs JSON where packages array entries
    have a numeric "pagerank" field. PKG_EDGE_CAP constant defined. Vendor files not in
    nodes[]. pkg: synthetic nodes not in hubs output. Confirmed:
    node agentmap.mjs --print | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); if(d.packages) console.log('packages ok:', d.packages.length); else console.log('no packages key');"
  </done>
</task>

<task type="auto">
  <name>Task 2: Inject package matches into --any router (CMP-05 --any routing)</name>
  <files>agentmap.mjs</files>
  <read_first>
    - agentmap.mjs lines 1748-1800 — full --any handler to find exact injection point
    - .planning/phases/16-integration-cli/16-RESEARCH.md lines 207-220 — Pattern 4 exact
      injection logic and CONTEXT.md order requirement (packages after file, before symbols)
    - .planning/phases/16-integration-cli/16-RESEARCH.md lines 305-315 — Pitfall 3 injection
      order warning
  </read_first>
  <action>
    LOCATION: Inside the --any handler's else-branch (after "const raw = arg('--any');" guard).

    STEP 1 — After line "const featNames = ..." (line 1764) and BEFORE the "if (fileKey)"
    block (line 1765), compute package matches:

      const pkgMatches = (data.packages || []).filter(p => {
        const ql = q; // q is already lowercased above
        return p.to.toLowerCase().includes(ql) || p.from.toLowerCase().includes(ql);
      });

    STEP 2 — In the fileKey branch (lines 1765-1774): add pkgMatches to the out() JSON
    object as "packages: pkgMatches" field. In prose, if pkgMatches.length > 0, print a
    line "[packages] N match for '{raw}':" followed by one line per match:
      "  {p.from} → {p.to} [{p.type}] {p.constraint}"

    STEP 3 — In the "else if (symHits.length || featNames.length)" branch (line 1775):
    change the condition to also trigger when pkgMatches.length > 0:
      else if (pkgMatches.length || symHits.length || featNames.length)
    Add pkgMatches to the out() JSON as "packages: pkgMatches". In prose, print package
    matches BEFORE symbol hits (per CONTEXT.md locked order: packages before symbols):
      if (pkgMatches.length) { console.log(`[packages] ${pkgMatches.length} match for "${raw}":`); ... }
      if (symHits.length) { ...existing symbol output... }

    STEP 4 — Do NOT change the candidates branch or the contentSearch fallback branch.
    Package matches only surface in the file-match and structure-match branches.

    CRITICAL: Per Pitfall 3 — package matches must appear BEFORE symbol matches in all
    output paths. Double-check prose output order after editing.

    CRITICAL: Per CONTEXT.md "low priority" decision — package matches are supplementary.
    They do not replace file or symbol matches; they are an additional field alongside them.
    Process exitCode is NOT set to 1 when only pkgMatches found (packages are a valid result).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    npm test passes (all pre-existing + all Plan 1 new tests green). Smoke checks:
    - On a project with composer.json: node agentmap.mjs --any laravel outputs package
      matches before any symbol matches in prose output.
    - node agentmap.mjs --any laravel --json emits JSON with "packages" array field.
    - node agentmap.mjs --any SomeClass still works (no package matches, symbols only).
    - node agentmap.mjs --any nonexistent still falls through to git-grep.
    - Plan 1 test/pagerank-packages.test.mjs GREEN (packages have pagerank field).
    - Plan 1 test/any-packages portion of packages-cli.test.mjs GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer.json package names → synthetic node keys | Package "to" field used as node key suffix; no filesystem access |
| --any query string → package filter | q is already lowercased from existing arg() call; no exec/eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering | pkg: synthetic node keys | accept | Keys are "pkg:" + p.to string from parsed composer.json; no shell execution; worst case is a malformed pagerank score on a weird package name |
| T-16-06 | Denial of Service | PageRank edge explosion | mitigate | PKG_EDGE_CAP = 1000 enforced per package; build() wraps edge block in try-catch; pathological composer.json can't cause infinite loop |
| T-16-07 | Tampering | --any query injected into package filter | accept | filter uses String.includes() on pre-lowercased strings; no regex, no exec, no shell |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Full phase gate (all plans complete):
- npm test — full suite green (200+ pre-existing + 4 new test files all passing)
- node agentmap.mjs --packages exits 0 with package list
- node agentmap.mjs --types exits 0 with PHP type summary
- node agentmap.mjs --legacy exits 0 with legacy warning list
- node agentmap.mjs --packages --json | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['command']=='packages'"
- node agentmap.mjs --print | python3 -c "import sys,json; d=json.load(sys.stdin); pkgs=d.get('packages',[]); print('packages with pagerank:', sum(1 for p in pkgs if isinstance(p.get('pagerank'),float)))"
- node agentmap.mjs --hubs (must still work — no regression)
- node agentmap.mjs --map (must still work — no regression)
- SCHEMA_VERSION in constants.mjs === 4 (grep -c "SCHEMA_VERSION = 4" src/Core/constants.mjs returns 1)
</verification>

<success_criteria>
- CMP-04: packages array in map.json has numeric pagerank per entry; PKG_EDGE_CAP enforced
- CMP-05 --any: package matches appear before symbol matches in --any output
- npm test: all tests green including all 4 new test files from Plan 1
- No behavior change to existing flags (--map, --hubs, --symbols, --find, --relates, --any
  non-package queries all work identically)
- SCHEMA_VERSION = 4 triggers auto-rebuild on stale v3 caches
</success_criteria>

<output>
Create .planning/phases/16-integration-cli/16-03-SUMMARY.md when done
</output>
