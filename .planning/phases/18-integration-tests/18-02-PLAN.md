---
phase: 18-integration-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - INTG-01
  - INTG-02
  - INTG-03
  - INTG-04

must_haves:
  truths:
    - "CI clones laravel/framework before `Run tests` on the node-20 matrix leg"
    - "Node 18 and 22 legs skip integration tests gracefully via existsSync guard"
    - "All existing CI steps still run unchanged"
    - "Clone step is conditioned on `matrix.node-version == 20` to avoid triple-cloning"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with laravel/framework clone step"
      contains: "Clone laravel/framework fixture"
  key_links:
    - from: ".github/workflows/ci.yml clone step"
      to: "tmp/eval/laravel-framework"
      via: "git clone --depth 1 ... tmp/eval/laravel-framework"
      pattern: "clone.*laravel"
    - from: "tmp/eval/laravel-framework (CI)"
      to: "test/integration-laravel.test.mjs"
      via: "existsSync(LARAVEL) guard passes when dir exists after clone"
      pattern: "existsSync"
---

<objective>
Add a `Clone laravel/framework fixture` step to .github/workflows/ci.yml so that
integration tests in test/integration-laravel.test.mjs run on the node-20 CI leg.

Purpose: Without the clone step the fixture is absent in CI and all integration
tests skip. Adding a depth-1 clone on one leg is the minimum viable CI coverage
for INTG-01 through INTG-04 without tripling network cost.

Output: .github/workflows/ci.yml with one new step inserted before `Run tests`,
conditioned on `matrix.node-version == 20`.
</objective>

<execution_context>
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/workflows/execute-plan.md
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-integration-tests/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert clone step into .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — read full file to locate the exact `Run tests` step
      and confirm indentation (2-space YAML)
  </read_first>
  <action>
Read .github/workflows/ci.yml in full. Locate the `Run tests` step block:

```
      # Black-box suite — drives the real CLI as a subprocess against temp repos.
      - name: Run tests
        run: npm test
```

Using Edit (scoped replacement), insert a new step immediately BEFORE that block.
The new step must use identical 6-space indentation as the surrounding steps:

      # Clone the laravel/framework fixture for integration tests (node-20 leg only —
      # other legs skip via existsSync guard in test/integration-laravel.test.mjs).
      - name: Clone laravel/framework fixture
        if: matrix.node-version == 20
        run: git clone --depth 1 https://github.com/laravel/framework tmp/eval/laravel-framework

The `if:` condition uses the unquoted integer `20` (not the string `'20'`) to match
the matrix value `node-version: [18, 20, 22]` exactly. Do NOT add quotes around 20.

No other lines in ci.yml should be modified. Do not reformat, reorder, or touch
any other step.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const text = fs.readFileSync('.github/workflows/ci.yml', 'utf8');
const hasClone = text.includes('Clone laravel/framework fixture');
const hasCondition = text.includes('matrix.node-version == 20');
const hasGitClone = text.includes('git clone --depth 1 https://github.com/laravel/framework tmp/eval/laravel-framework');
if (!hasClone || !hasCondition || !hasGitClone) {
  console.error('FAIL: missing expected clone step content');
  process.exit(1);
}
console.log('PASS: clone step present with correct condition and command');
"</automated>
  </verify>
  <done>
    - .github/workflows/ci.yml contains `Clone laravel/framework fixture` step
    - Step has `if: matrix.node-version == 20`
    - Step runs `git clone --depth 1 https://github.com/laravel/framework tmp/eval/laravel-framework`
    - Step appears before `Run tests` in the file
    - All other steps unchanged
    - YAML is valid (no indentation errors)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → github.com/laravel/framework | Outbound git clone over HTTPS during CI |
| cloned fixture → test runner | Fixture files read by CLI subprocess; not executed as code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | laravel/framework clone in CI | accept | Public read-only repo; depth-1 clone; fixture only read by CLI, not executed |
| T-18-04 | Denial of Service | CI clone adds ~30s network time | accept | Single leg only (node-20); acceptable CI cost for integration coverage |
| T-18-05 | Spoofing | github.com HTTPS endpoint | accept | GitHub TLS cert; standard git clone; no custom hosts or mirrors |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; only ci.yml edit |
</threat_model>

<verification>
After editing ci.yml:

1. Verify YAML syntax: `node -e "require('fs').readFileSync('.github/workflows/ci.yml','utf8')" && echo OK`
2. Confirm clone step appears before Run tests step in file order
3. Confirm `if: matrix.node-version == 20` is present
4. Confirm no other steps were modified (git diff --stat should show ci.yml only)
</verification>

<success_criteria>
- .github/workflows/ci.yml is valid YAML with no syntax errors
- Clone step present with correct `if:` condition and `git clone --depth 1` command
- On node-20 CI leg: fixture cloned → integration tests run and assert INTG-01–04
- On node-18 and node-22 legs: no clone → existsSync guard triggers → tests skip, not fail
- All pre-existing CI steps (Checkout, Setup Node, Install dependencies, Run tests, Smoke, Audit, Pack) unchanged
</success_criteria>

<output>
Create `.planning/phases/18-integration-tests/18-02-SUMMARY.md` when done.
</output>
