---
phase: 18-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration-laravel.test.mjs
autonomous: true
requirements:
  - INTG-01
  - INTG-02
  - INTG-03
  - INTG-04

must_haves:
  truths:
    - "Running `npm test` executes integration-laravel.test.mjs when fixture exists"
    - "`--packages` against laravel/framework exits 0 and stdout contains `laravel/framework`"
    - "`--types` against laravel/framework exits 0 and stdout is non-empty"
    - "`--legacy` against laravel/framework exits 0 (graceful, no crash)"
    - "Tests skip (not fail) when tmp/eval/laravel-framework is absent"
  artifacts:
    - path: "test/integration-laravel.test.mjs"
      provides: "Integration test suite against real laravel/framework fixture"
      min_lines: 50
  key_links:
    - from: "test/integration-laravel.test.mjs"
      to: "test/helpers.mjs"
      via: "import { run } from './helpers.mjs'"
      pattern: "from.*helpers"
    - from: "test/integration-laravel.test.mjs"
      to: "tmp/eval/laravel-framework"
      via: "LARAVEL constant resolved via fileURLToPath + dirname + join"
      pattern: "tmp.*eval.*laravel"
---

<objective>
Write test/integration-laravel.test.mjs — integration tests that run the real CLI
against the existing tmp/eval/laravel-framework/ fixture and assert structural
invariants on --packages, --types, and --legacy output.

Purpose: Give CI confidence that CLI flags work against a real ~2000-file Laravel
repo, not just toy fixtures. Covers INTG-01 through INTG-04.

Output: test/integration-laravel.test.mjs (picked up automatically by the Phase 17
fixed glob `test/**/*.test.mjs`)
</objective>

<execution_context>
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/workflows/execute-plan.md
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@test/helpers.mjs
@test/packages-cli.test.mjs
@test/types-cli.test.mjs
@test/legacy-cli.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write test/integration-laravel.test.mjs</name>
  <files>test/integration-laravel.test.mjs</files>
  <read_first>
    - test/helpers.mjs — `run(dir, ...args)` signature, exports list
    - test/packages-cli.test.mjs — import block pattern, assert.equal / assert.match usage
    - test/types-cli.test.mjs — same pattern
    - test/legacy-cli.test.mjs — same pattern
  </read_first>
  <action>
Create test/integration-laravel.test.mjs with the following structure (per INTG-01–04):

SPDX header comment: `// SPDX-License-Identifier: MIT`
Brief comment block explaining this is the integration suite against the real
laravel/framework fixture, that tests skip when the fixture is absent, and that
the 60s timeout covers the large-repo CLI run.

Imports (match project pattern exactly):
  - `test` from `node:test`
  - default `assert` from `node:assert/strict`
  - `existsSync` from `node:fs`
  - `join`, `dirname` from `node:path`
  - `fileURLToPath` from `node:url`
  - `run` from `./helpers.mjs`

Constants:
  - `HERE = dirname(fileURLToPath(import.meta.url))`
  - `LARAVEL = join(HERE, "..", "tmp", "eval", "laravel-framework")`

Four tests, each with `{ timeout: 60_000 }` as the second argument to `test()`:

1. Test name: `"--packages lists laravel/framework package (INTG-01)"`:
   - If `!existsSync(LARAVEL)`: call `ctx.skip("laravel/framework fixture absent — run: git clone --depth 1 https://github.com/laravel/framework tmp/eval/laravel-framework")`
   - `const r = run(LARAVEL, "--packages")`
   - `assert.equal(r.status, 0, ...)`
   - `assert.match(r.stdout, /laravel\/framework/)`

2. Test name: `"--types returns non-empty output for laravel/framework (INTG-02)"`:
   - Same skip guard pattern
   - `const r = run(LARAVEL, "--types")`
   - `assert.equal(r.status, 0, ...)`
   - `assert.ok(r.stdout.length > 0, "expected non-empty --types output")`

3. Test name: `"--legacy exits 0 against laravel/framework (INTG-03)"`:
   - Same skip guard pattern
   - `const r = run(LARAVEL, "--legacy")`
   - `assert.equal(r.status, 0, ...)` — no stdout assertion (may or may not have warnings)

4. Test name: `"integration tests skip gracefully when fixture is absent (INTG-04)"`:
   - This test always runs (no skip guard here)
   - `assert.ok(typeof LARAVEL === "string", "LARAVEL constant must be a string path")`
   - `assert.ok(LARAVEL.endsWith("laravel-framework"), "LARAVEL path must end with laravel-framework")`
   - Comment: documents that tests 1-3 use existsSync(LARAVEL) to skip when absent

Note: the skip guard uses `ctx` — the test callback signature must be
`test("...", { timeout: 60_000 }, (ctx) => { ... })` for the `ctx.skip()` call
to work. Use this form for tests 1-3. Test 4 uses `() => { ... }` (no ctx needed).

Do NOT use `test.skip()` at module level — use `ctx.skip()` inside the callback
so node:test reports these as skipped rather than excluded from the count.
  </action>
  <verify>
    <automated>node --test test/integration-laravel.test.mjs 2>&1 | head -30</automated>
  </verify>
  <done>
    - File exists at test/integration-laravel.test.mjs
    - `node --test test/integration-laravel.test.mjs` runs without a parse error
    - When fixture is absent: all 4 tests complete (3 skipped, 1 pass) — exit code 0
    - When fixture is present: all 4 tests pass with exit 0 — exit code 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → CLI subprocess | `run()` spawns node subprocess; stdout/stderr captured, not eval'd |
| CI → github.com/laravel/framework | Network clone in CI; shallow depth-1 clone limits exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | tmp/eval/laravel-framework clone | accept | Read-only fixture use; no write operations on fixture dir during tests |
| T-18-02 | Denial of Service | 60s test timeout | mitigate | `{ timeout: 60_000 }` on each test; node:test kills subprocess on timeout |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; zero-dep test using node builtins only |
</threat_model>

<verification>
After writing the file:

1. `node --test test/integration-laravel.test.mjs` — must exit 0 (skips are OK, failures are not)
2. `node -e "import('./test/integration-laravel.test.mjs')"` — must not throw a syntax/import error
3. Confirm `LARAVEL` constant resolves to an absolute path ending in `laravel-framework`
</verification>

<success_criteria>
- test/integration-laravel.test.mjs exists and is syntactically valid ESM
- All 4 tests complete without uncaught errors (skipped counts as complete)
- Assertions match INTG-01: `laravel/framework` in --packages stdout
- Assertions match INTG-02: non-empty --types stdout
- Assertions match INTG-03: exit 0 for --legacy
- Assertions match INTG-04: skip path exercised when fixture absent (ctx.skip)
</success_criteria>

<output>
Create `.planning/phases/18-integration-tests/18-01-SUMMARY.md` when done.
</output>
