---
id: 17-01
phase: 17-ci-glob-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CI-01
  - CI-02
must_haves:
  truths:
    - "CI Run tests step uses `npm test`"
    - "No other steps in ci.yml were changed"
    - "package.json test script retains the correct two-glob pattern"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with correct test invocation"
      contains: "npm test"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "`npm test` delegates to scripts.test — two-glob pattern covers test/ and test/**/"
      pattern: "npm test"
---

<objective>
Fix CI workflow so all 256 tests run on every push and PR across Node 18/20/22 matrix.

Purpose: Line 32 of ci.yml uses `node --test test/*.test.mjs` — a non-recursive shell glob that silently skips test/vue-sfc/ (9 files, 43 tests). Changing to `npm test` delegates glob expansion to package.json scripts.test, which already has the correct two-glob pattern. CI stays automatically in sync as test subdirectories are added.

Output: Modified .github/workflows/ci.yml with Run tests step using `npm test`.
</objective>

<execution_context>
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/workflows/execute-plan.md
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="17-01-T01" type="auto">
  <name>Task 1: Fix CI Run tests step to use npm test</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    In .github/workflows/ci.yml, replace the `run:` value on the "Run tests" step (currently `node --test test/*.test.mjs`) with `npm test`.

    The "Run tests" step is at line 31-32. Only that `run:` line changes — no other lines in the file are modified. The `npm ci` install step before it already ensures node_modules are present, so `npm test` will resolve correctly.

    Per CI-01 and CI-02: `npm test` invokes `node --test test/*.test.mjs test/**/*.test.mjs` from package.json, which covers both the root test/ directory and test/vue-sfc/ subdirectory, giving CI the same 256-test count as local runs.
  </action>
  <verify>
    <automated>grep -n 'npm test' .github/workflows/ci.yml</automated>
  </verify>
  <done>
    - .github/workflows/ci.yml line 32 reads `run: npm test`
    - `grep -c 'node --test test/\*\.test\.mjs' .github/workflows/ci.yml` returns 0
    - All other lines in ci.yml are unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → npm scripts | npm test executes package.json scripts.test; no untrusted input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | .github/workflows/ci.yml | accept | Single-line change; diff reviewed on PR before merge |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After applying the change:

1. `grep 'npm test' .github/workflows/ci.yml` — must match line 32
2. `grep -c 'node --test test/\*' .github/workflows/ci.yml` — must return 0
3. `git diff .github/workflows/ci.yml` — diff must show exactly one line changed (the run: value on the Run tests step)
4. Push to a branch and confirm CI matrix shows 256 tests across Node 18/20/22
</verification>

<success_criteria>
- CI log shows 256 tests (not 213) on every push and PR across Node 18/20/22 matrix
- test/vue-sfc/ tests appear in CI output with no silent skip
- CI exit code is non-zero when any test fails
- Local `npm test` and CI test count match
</success_criteria>

<output>
Create `.planning/phases/17-ci-glob-fix/17-01-SUMMARY.md` when done.
</output>
