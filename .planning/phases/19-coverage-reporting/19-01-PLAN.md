---
phase: 19-coverage-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
autonomous: true
requirements:
  - COV-01
  - COV-03

must_haves:
  truths:
    - "`c8` is listed in devDependencies at exactly `11.0.0` (no caret)"
    - "`package-lock.json` reflects the c8 install (lockfile updated)"
    - "`coverage/` appears in `.gitignore` so generated reports are never committed"
  artifacts:
    - path: "package.json"
      provides: "c8 devDependency entry"
      contains: '"c8": "11.0.0"'
    - path: "package-lock.json"
      provides: "locked c8 install"
      contains: "c8"
    - path: ".gitignore"
      provides: "coverage/ exclusion"
      contains: "coverage/"
  key_links:
    - from: "package.json devDependencies"
      to: "package-lock.json"
      via: "npm install — updates lockfile after package.json edit"
      pattern: '"c8"'
---

<objective>
Install c8@11.0.0 as a devDependency and exclude the generated coverage/ directory from git.

Purpose: c8 is the only coverage tool that captures V8 coverage across the execFileSync subprocess boundary used by test/helpers.mjs — making src/Core/ modules visible in reports. The exact pin avoids drift. Gitignoring coverage/ prevents accidental commits of generated HTML/lcov artifacts.

Output: Updated package.json, package-lock.json (lockfile reflects c8), and .gitignore with coverage/ entry.
</objective>

<execution_context>
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/workflows/execute-plan.md
@/home/dstwn/Workspaces/agentmap-php/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add c8 to devDependencies and update lockfile</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json — confirm `devDependencies` key is absent (no existing block) and that `dependencies` ends at the closing brace before keywords. The new block goes between `dependencies` and `keywords`.</read_first>
  <action>
In package.json, add a `"devDependencies"` block with exactly one entry: `"c8": "11.0.0"` (no caret, no tilde — exact pin per COV-01 research constraint). Insert it after the `"dependencies"` block and before `"keywords"`. Then run `npm install` (not `npm ci`) to write the c8 entry into package-lock.json. Do not add any other packages. Do not modify the `"dependencies"` block.
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const v=p.devDependencies&&p.devDependencies.c8; if(v!=='11.0.0') throw new Error('Expected c8@11.0.0, got: '+v); console.log('OK: c8='+v)"</automated>
  </verify>
  <done>package.json has `"devDependencies": { "c8": "11.0.0" }` and package-lock.json contains a c8 entry at that version.</done>
</task>

<task type="auto">
  <name>Task 2: Add coverage/ to .gitignore</name>
  <files>.gitignore</files>
  <read_first>.gitignore — confirm `coverage/` is not already present before appending.</read_first>
  <action>
Append `coverage/` on a new line at the end of .gitignore. The entry must end with a trailing slash (directory glob). Do not remove or reorder any existing entries.
  </action>
  <verify>
    <automated>grep -c '^coverage/$' .gitignore</automated>
  </verify>
  <done>.gitignore contains exactly one `coverage/` line; running `git status` after generating a coverage/ directory would show it as untracked-but-ignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | c8 package fetched from npmjs.com during npm install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-19-01 | Tampering | npm install (c8@11.0.0) | mitigate | Exact version pin `11.0.0` (no caret) prevents silent upgrades to a compromised release; lockfile commit locks the resolved tree |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | c8 is a well-known, actively maintained package (npmjs.com/package/c8, >10M weekly downloads); exact pin + lockfile commit |
</threat_model>

<verification>
1. `node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(p.devDependencies)"` — must print `{ c8: '11.0.0' }`
2. `grep c8 package-lock.json | head -3` — must show c8 entries
3. `grep '^coverage/$' .gitignore` — must return one match
4. `npm test` — existing test suite must still pass (no regressions from adding devDependency)
</verification>

<success_criteria>
- package.json devDependencies contains `"c8": "11.0.0"` (exact pin, no range prefix)
- package-lock.json updated to include c8 resolved tree
- .gitignore contains `coverage/`
- `npm test` passes without regression
</success_criteria>

<output>
Create `.planning/phases/19-coverage-reporting/19-01-SUMMARY.md` when done.

Note for SUMMARY: COV-03 (threshold deferred to v1.4) — no `--lines` / `--branches` flags are used anywhere. Threshold enforcement is a deliberate deferral; the baseline coverage % is unknown until the first c8 run completes. This is intentional per research constraints (those flags do not exist on Node 18/20 anyway).
</output>
